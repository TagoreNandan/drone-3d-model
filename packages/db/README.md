# packages/db — placeholder, not implemented

Generated by Better Fullstack as a "Standalone database stack part," but it's incomplete: there's no `package.json`, no scripts, and no Prisma/Drizzle schema here.

The actual Postgres/TimescaleDB setup for this project lives elsewhere:

- **Container definition**: root `docker-compose.yml` (service `timescaledb`, published on host port 5433 — see root `README.md` for why)
- **Schema**: `apps/server/sql/schema/001_schema.sql`
- **Queries**: `apps/server/sql/queries/*.sql` (sqlc annotations, not yet code-generated)

The root `package.json` / `turbo.json` reference `@gcs/db` scripts (`db:start`, `db:watch`, `db:stop`, `db:down`) that **do not currently work**, since this package has no `package.json` to define them. Either:

1. Add a `package.json` here with `db:start` → `docker compose up -d`, `db:stop` → `docker compose stop`, `db:down` → `docker compose down`, etc. (referencing the root `docker-compose.yml`), or
2. Remove the dead `@gcs/db` references from the root `package.json`/`turbo.json` and just use `docker compose` directly / `./run.sh`.

Nothing here needs installing or running as-is — it's inert until one of the above happens.

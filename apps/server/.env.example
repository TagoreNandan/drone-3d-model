# Application settings
HOST=0.0.0.0
PORT=8080

# Database settings (if using GORM or sqlc)
DATABASE_URL=postgres://gcs:gcs@localhost:5433/gcs?sslmode=disable

# gRPC settings (if using gRPC)
GRPC_PORT=50051

# Logging level
LOG_LEVEL=debug
